{
  "name": "hll-bots",
  "version": "1.0.0",
  "description": "Bot para CRCON/HLL com ranking top kills e anuncio automatico",
  "main": "src/bot.js",
  "scripts": {
    "bot": "node src/bot.js",
    "clear": "node src/clear.js",
    "restart": "SERVICE_NAME=\"${SERVICE_NAME:-hll-top-bot}\"; git pull --ff-only && sudo systemctl restart \"$SERVICE_NAME\" && sudo systemctl status \"$SERVICE_NAME\" --no-pager",
    "stop": "SERVICE_NAME=\"${SERVICE_NAME:-hll-top-bot}\"; sudo systemctl stop \"$SERVICE_NAME\" && sudo systemctl status \"$SERVICE_NAME\" --no-pager || true"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
